import { ArrowRight, CircleAlert } from 'lucide-react';
import Header from '@/components/Header'; import { Footer } from '@/components/Footer'; import { LocalizedLink as Link } from '@/components/LocalizedLink';
import { PORSCHE_HUB_PATH } from '@/data/porscheArchitecture'; import { PORSCHE_PROBLEMS_PATH, porscheProblemGuides } from '@/data/porscheProblemGuides';
import { useSeo } from '@/hooks/use-seo'; import { buildBreadcrumb, buildWebPage, pageGraph, SITE_URL } from '@/lib/schema';
const PorscheProblemsIndex = () => { const url = `${SITE_URL}${PORSCHE_PROBLEMS_PATH}`; useSeo({ title: 'Porsche Problems & Warning Signs | Digi-Tec', description: 'Diagnostic guides for Porsche transmission, engine, cooling, suspension, brake, battery, AC and model-specific symptoms.', canonical: url, jsonLd: pageGraph([buildWebPage({ url, name: 'Porsche Problems and Warning Signs', description: 'Porsche symptom guides for owners.', breadcrumbId: `${url}#breadcrumb` }), buildBreadcrumb(url, [{ name: 'Home', url: SITE_URL }, { name: 'Porsche', url: `${SITE_URL}${PORSCHE_HUB_PATH}` }, { name: 'Problems', url }])]) }); return <div className="min-h-screen bg-black text-off-white"><Header /><main><nav aria-label="Breadcrumb" className="mx-auto max-w-7xl px-4 pt-6 text-sm text-white/50 sm:px-6"><Link to="/">Home</Link> / <Link to={PORSCHE_HUB_PATH}>Porsche</Link> / <span className="text-white">Problems</span></nav><section className="py-14 sm:py-20"><div className="mx-auto max-w-7xl px-4 sm:px-6"><CircleAlert className="h-9 w-9 text-burnt-orange" /><h1 className="mt-5 text-4xl font-black sm:text-6xl">Porsche Problems & Warning Signs</h1><p className="mt-6 max-w-3xl text-lg leading-relaxed text-white/65">Choose the symptom you have noticed. These guides explain possibilities and driving risk; they do not diagnose a vehicle remotely.</p><div className="mt-10 grid gap-5 md:grid-cols-2 lg:grid-cols-3">{porscheProblemGuides.map((item) => <Link key={item.slug} to={`${PORSCHE_PROBLEMS_PATH}/${item.slug}`} className="card-premium group rounded-2xl p-6"><h2 className="text-lg font-black group-hover:text-burnt-orange">{item.name}</h2><p className="mt-3 text-sm leading-relaxed text-white/55">{item.summary}</p><span className="mt-5 inline-flex items-center gap-1 text-sm font-semibold text-burnt-orange">Read diagnostic guide <ArrowRight className="h-4 w-4" /></span></Link>)}</div></div></section></main><Footer /></div>; }; export default PorscheProblemsIndex;
